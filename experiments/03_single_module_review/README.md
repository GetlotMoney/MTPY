# REV-MOD 单模块复核

本目录用于 `REV-MOD-xxx` 实验，复核 `near_tie`、`win` 或用户指定的单模块候选。

复核实验必须写清：

- 原始 `MOD` 实验 ID。
- 继承的代码与配置。
- 预注册 seed 列表。
- 已有 seed 和需要补跑的 seed。
- 所有 seed 的 U / S / H / ZS / 最佳轮次 / 日志路径。
- 是否按候选 seed 最大 `H` 作为项目选型参考。

不允许事后临时挑 seed 当成正式结论。

