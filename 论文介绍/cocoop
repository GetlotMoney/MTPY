cocoop论文简介：代码地址：c:\Users\Administrator\Desktop\项目\CoOp-main
下面重新整理，公式全部用 LaTeX。讲的是 **CoCoOp: Conditional Context Optimization** 的方法部分。

**方法总览**

这篇论文的方法可以分成三步：

1. **CLIP zero-shot 分类**：人工 prompt 生成类别文本特征。
2. **CoOp**：把 prompt 中的上下文词改成可学习向量。
3. **CoCoOp**：让 prompt 根据每张输入图像动态变化，提升 unseen class 泛化。

---

**1. CLIP 的分类公式**

CLIP 对一张图像 \(x\) 的分类概率定义为：

$$
p(y \mid x)
=
\frac{
\exp\left(\mathrm{sim}(x, w_y) / \tau\right)
}{
\sum_{i=1}^{K}
\exp\left(\mathrm{sim}(x, w_i) / \tau\right)
}
\tag{1}
$$

**符号解释**

| 符号 | 含义 |
|---|---|
| \(x\) | 图像编码器输出的图像特征 |
| \(y\) | 真实类别或待预测类别 |
| \(K\) | 类别总数 |
| \(w_i\) | 第 \(i\) 个类别的文本特征，也可看作分类器权重 |
| \(w_y\) | 目标类别 \(y\) 的文本特征 |
| \(\mathrm{sim}(\cdot,\cdot)\) | 余弦相似度 |
| \(\tau\) | temperature 温度参数 |
| \(p(y \mid x)\) | 图像 \(x\) 属于类别 \(y\) 的概率 |

CLIP 的关键点是：类别权重 \(w_i\) 不是随机初始化的分类头，而是由文本编码器生成的。

例如类别是 `dog`，prompt 是：

$$
\text{``a photo of a dog''}
$$

经过 CLIP text encoder 后得到：

$$
w_{\text{dog}}
$$

所以 CLIP 分类本质是：

$$
\text{图像特征} \quad x
\quad \text{和} \quad
\text{类别文本特征} \quad w_i
\quad \text{做相似度匹配}
$$

---

**2. CoOp：上下文优化**

人工 prompt 例如：

$$
\text{``a photo of a } \{\text{class}\}\text{''}
$$

其中：

$$
\text{``a photo of a''}
$$

是上下文 context。

CoOp 不再手工设计这些词，而是把它们替换成可学习向量：

$$
\{v_1, v_2, \dots, v_M\}
$$

**符号解释**

| 符号 | 含义 |
|---|---|
| \(M\) | context token 的数量 |
| \(v_m\) | 第 \(m\) 个可学习 context 向量 |
| \(c_i\) | 第 \(i\) 个类别名的词嵌入 |
| \(t_i\) | 第 \(i\) 个类别的完整 prompt |

CoOp 中，第 \(i\) 类的 prompt 表示为：

$$
t_i = \{v_1, v_2, \dots, v_M, c_i\}
$$

也就是：

$$
[V_1][V_2]\dots[V_M][\text{CLASS}_i]
$$

这里的 \(v_1,\dots,v_M\) 是连续向量，不一定对应真实单词。

---

**CoOp 的分类公式**

CoOp 使用文本编码器 \(g(\cdot)\) 编码 prompt：

$$
p(y \mid x)
=
\frac{
\exp\left(\mathrm{sim}(x, g(t_y)) / \tau\right)
}{
\sum_{i=1}^{K}
\exp\left(\mathrm{sim}(x, g(t_i)) / \tau\right)
}
\tag{2}
$$

**符号解释**

| 符号 | 含义 |
|---|---|
| \(g(\cdot)\) | CLIP text encoder |
| \(t_i\) | 第 \(i\) 类的可学习 prompt |
| \(g(t_i)\) | 第 \(i\) 类 prompt 的文本特征 |
| \(x\) | 图像特征 |
| \(\tau\) | 温度参数 |

CLIP 和 CoOp 的区别是：

$$
\text{CLIP: } w_i
$$

来自人工 prompt。

$$
\text{CoOp: } g(t_i)
$$

来自可学习 prompt。

CoOp 训练时更新的是：

$$
v_1, v_2, \dots, v_M
$$

CLIP 的 image encoder 和 text encoder 保持冻结。

---

**CoOp 的问题**

CoOp 学到的是静态 prompt。

也就是说，不管输入图片是什么，prompt 都一样：

$$
t_i = \{v_1, v_2, \dots, v_M, c_i\}
$$

这会导致它很容易适配 base classes，但对 unseen classes 泛化不好。

所以论文提出 CoCoOp。

---

**3. CoCoOp：条件上下文优化**

CoCoOp 的核心思想是：

> prompt 应该根据每张图像动态变化。

它引入一个轻量网络，叫 Meta-Net：

$$
h_\theta(\cdot)
$$

Meta-Net 根据图像特征 \(x\) 生成一个条件向量：

$$
\pi = h_\theta(x)
$$

**符号解释**

| 符号 | 含义 |
|---|---|
| \(h_\theta\) | Meta-Net |
| \(\theta\) | Meta-Net 的参数 |
| \(x\) | 图像特征 |
| \(\pi\) | 由图像 \(x\) 生成的条件 token / 偏移向量 |

然后 CoCoOp 把这个 \(\pi\) 加到每个 context token 上：

$$
v_m(x) = v_m + \pi
$$

其中：

$$
m \in \{1,2,\dots,M\}
$$

**符号解释**

| 符号 | 含义 |
|---|---|
| \(v_m\) | 原始可学习 context token |
| \(v_m(x)\) | 针对图像 \(x\) 的动态 context token |
| \(\pi\) | 图像条件偏移 |
| \(M\) | context token 数量 |

这一步的意思是：

$$
v_1(x) = v_1 + \pi
$$

$$
v_2(x) = v_2 + \pi
$$

$$
\dots
$$

$$
v_M(x) = v_M + \pi
$$

所以同一个类别，在不同图像下会有不同 prompt。

---

**CoCoOp 的动态 prompt**

第 \(i\) 类针对图像 \(x\) 的 prompt 是：

$$
t_i(x)
=
\{v_1(x), v_2(x), \dots, v_M(x), c_i\}
$$

展开就是：

$$
t_i(x)
=
\{v_1+\pi, v_2+\pi, \dots, v_M+\pi, c_i\}
$$

其中：

$$
\pi = h_\theta(x)
$$

**含义**

CoOp 是：

$$
[V_1][V_2]\dots[V_M][\text{CLASS}_i]
$$

CoCoOp 是：

$$
[V_1+\pi(x)][V_2+\pi(x)]\dots[V_M+\pi(x)][\text{CLASS}_i]
$$

所以 CoCoOp 的 prompt 是 **instance-conditional prompt**，即“样本条件 prompt”。

---

**CoCoOp 的分类公式**

CoCoOp 的预测概率为：

$$
p(y \mid x)
=
\frac{
\exp\left(\mathrm{sim}(x, g(t_y(x))) / \tau\right)
}{
\sum_{i=1}^{K}
\exp\left(\mathrm{sim}(x, g(t_i(x))) / \tau\right)
}
\tag{3}
$$

**符号解释**

| 符号 | 含义 |
|---|---|
| \(t_i(x)\) | 根据图像 \(x\) 生成的第 \(i\) 类动态 prompt |
| \(g(t_i(x))\) | 动态 prompt 经过 text encoder 得到的文本特征 |
| \(x\) | 图像特征 |
| \(K\) | 类别数 |
| \(\tau\) | 温度参数 |
| \(p(y \mid x)\) | 图像 \(x\) 属于类别 \(y\) 的概率 |

和 CoOp 相比，关键变化是：

$$
g(t_i)
\quad \rightarrow \quad
g(t_i(x))
$$

CoOp 的文本分类器是固定的。

CoCoOp 的文本分类器会根据当前输入图像变化。

---

**4. 训练目标**

论文方法部分没有单独编号 loss，但它使用交叉熵训练。

对单个样本：

$$
\mathcal{L}
=
-\log p(y \mid x)
$$

对一个 batch：

$$
\mathcal{L}
=
-\frac{1}{N}
\sum_{n=1}^{N}
\log p(y_n \mid x_n)
$$

**符号解释**

| 符号 | 含义 |
|---|---|
| \(\mathcal{L}\) | 训练损失 |
| \(N\) | batch size |
| \(x_n\) | 第 \(n\) 张图像的特征 |
| \(y_n\) | 第 \(n\) 张图像的真实标签 |
| \(p(y_n \mid x_n)\) | 模型预测真实类别的概率 |

训练时更新：

$$
\{v_m\}_{m=1}^{M}
$$

以及：

$$
\theta
$$

也就是 context vectors 和 Meta-Net 参数。

冻结：

$$
\text{CLIP image encoder}
$$

$$
\text{CLIP text encoder}
$$

---

**5. Meta-Net 结构**

论文中 Meta-Net 是一个两层 bottleneck MLP：

$$
h_\theta(x)
=
W_2 \, \sigma(W_1 x)
$$

其中 \(\sigma\) 是 ReLU。

如果图像特征维度是 \(d\)，Meta-Net 大致为：

$$
\mathbb{R}^{d}
\rightarrow
\mathbb{R}^{d/16}
\rightarrow
\mathbb{R}^{d}
$$

**符号解释**

| 符号 | 含义 |
|---|---|
| \(d\) | CLIP 特征维度 |
| \(W_1\) | 第一层线性层参数 |
| \(W_2\) | 第二层线性层参数 |
| \(\sigma\) | ReLU 激活函数 |
| \(h_\theta(x)\) | Meta-Net 输出的条件向量 \(\pi\) |

输出维度必须和 \(v_m\) 一样，因为要相加：

$$
v_m(x)=v_m+\pi
$$

---

**6. 三种方法对比**

**CLIP**

$$
t_i = \text{``a photo of a } c_i \text{''}
$$

人工 prompt，不训练。

**CoOp**

$$
t_i = \{v_1, v_2, \dots, v_M, c_i\}
$$

静态可学习 prompt。

**CoCoOp**

$$
t_i(x) = \{v_1+\pi, v_2+\pi, \dots, v_M+\pi, c_i\}
$$

其中：

$$
\pi = h_\theta(x)
$$

动态可学习 prompt。

| 方法 | prompt 是否可学习 | prompt 是否随图像变化 | 更新参数 |
|---|---|---|---|
| CLIP | 否 | 否 | 无 |
| CoOp | 是 | 否 | \(v_1,\dots,v_M\) |
| CoCoOp | 是 | 是 | \(v_1,\dots,v_M,\theta\) |

---

**一句话总结**

CoCoOp 的方法可以写成：

$$
\pi = h_\theta(x)
$$

$$
v_m(x) = v_m + \pi
$$

$$
t_i(x)=\{v_1(x),\dots,v_M(x),c_i\}
$$

$$
p(y \mid x)
=
\frac{
\exp\left(\mathrm{sim}(x, g(t_y(x))) / \tau\right)
}{
\sum_{i=1}^{K}
\exp\left(\mathrm{sim}(x, g(t_i(x))) / \tau\right)
}
$$

它的核心是：**给每张图像生成一个动态 prompt 偏移，让 prompt 不再只适配 base classes，而是更关注当前输入实例，从而提升 unseen classes 泛化能力。**

看了，主要链路我已经读完了。你说得对：你这个 DVSR 不是“视觉端 adapter”，而是**文本端 adapter**。

关键位置：

- [MyModel.py](</C:/Users/Administrator/Desktop/项目/DVSR/model/MyModel.py:98>)：`Adapter` 是 `Linear-ReLU-Linear` bottleneck。
- [MyModel.py](</C:/Users/Administrator/Desktop/项目/DVSR/model/MyModel.py:611>)：`self.text_adapter = Adapter(...)`。
- [MyModel.py](</C:/Users/Administrator/Desktop/项目/DVSR/model/MyModel.py:669>)：`get_adapted_seen_text()` 只增强 seen 文本。
- [MyModel.py](</C:/Users/Administrator/Desktop/项目/DVSR/model/MyModel.py:763>)：forward 里构造 `all_text = seen_adapted + unseen_text_embeds`。
- [train_VGSR_CUB.py](</C:/Users/Administrator/Desktop/项目/DVSR/train_VGSR_CUB.py:230>)：Claude/GPT 描述先经 CLIP text encoder 编成 `[200,768]`。
- [train_VGSR_CUB.py](</C:/Users/Administrator/Desktop/项目/DVSR/train_VGSR_CUB.py:321>)：seen/unseen 都用 Claude/GPT 文本 embedding 传进模型。

你当前真实框架应该画成这样：

```mermaid
flowchart LR
    I["图像"] --> CLIPV["CLIP Visual Encoder 冻结"]
    CLIPV --> CLS["CLS [B,768]"]
    CLIPV --> P["patches [B,576,768]"]

    C["类别名"] --> Claude["Claude/GPT 描述"]
    Claude --> CLIPT["CLIP Text Encoder 冻结"]
    CLIPT --> TE["text embeds [200,768]"]

    TE --> Seen["seen text [150,768]"]
    TE --> Unseen["unseen text [50,768]"]

    Seen --> TA["Text Adapter 可训练"]
    TA --> SeenA["adapted seen text"]
    Unseen --> UnseenF["frozen unseen text"]

    SeenA --> ALL["all_text [200,768]"]
    UnseenF --> ALL

    CLS --> Base["base_logits = CLS · all_text"]
    ALL --> Base

    P --> CMT["CrossModalTransformer"]
    ALL --> CMT
    CMT --> Local["local_score"]

    Base --> Add["base_logits + local_weight * local_score"]
    Local --> Add
    Add --> Out["GZSL logits"]
```

所以 CoCoOp 融合进去时，不应该按我之前那个“视觉 adapter + CoCoOp”的图讲。更贴合你代码的版本应该是：

**Claude 文本 embedding + 静态 Text Adapter + 图像条件化 Text Residual**

也就是在 `all_text` 上加一个由图像生成的条件残差：

```python
pi = meta_net(cls_token)                  # [B, 768]
all_text_dyn = all_text[None, :, :] + beta * pi[:, None, :]
logits = cosine(cls_token, all_text_dyn)
```

推荐命名：

**Conditional Text Adapter for DVSR**

最小改法是：先只把 CoCoOp 融进 `base_logits`，不动 `CrossModalTransformer`，风险最低。完整改法是让 `CrossModalTransformer` 也支持 `[B, 200, 768]` 的动态图像条件文本，但这会动到 [MyModel.py](</C:/Users/Administrator/Desktop/项目/DVSR/model/MyModel.py:506>) 后面的 `txt_batch` 逻辑，改动更大。

我现在的判断是：你的论文创新可以表述为：

> 在 LLM 生成的类别语义嵌入基础上，本文引入图像条件化文本适配模块，使静态文本 adapter 从 seen-class-specific adaptation 扩展为 instance-conditioned semantic adaptation，从而缓解 seen/unseen 文本流形错位与固定语义提示泛化不足的问题。

更直白一点：**你不是把 CoCoOp 原封不动搬进 token prompt，而是把 CoCoOp 的“instance-conditioned prompt”思想迁移到 DVSR 的文本特征 adapter 上。**这条路是合理的，而且比原版 CoCoOp 更贴合你当前代码。